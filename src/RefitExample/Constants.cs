using System;

namespace RefitExample
{
    public static class Constants
    {           
        public static string API_BASE_URL = "http://localhost:3000";
        public static string API_POSTS_PREFIX = "/posts";

        public static TimeSpan TIMEOUT = TimeSpan.FromSeconds(2);
    }
}
