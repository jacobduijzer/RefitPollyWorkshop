{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "jsonwebtoken": "^8.4.0"
  },
  "devDependencies": {
    "json-server": "^0.14.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-simple": "json-server --watch ./db.json",
    "start-simple-delayed": "json-server --watch ./db.json -d 2000",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
