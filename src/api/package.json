{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "json-server": "^0.14.2",
    "jsonwebtoken": "^8.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./db.json",
    "start-delayed": "json-server --watch ./db.json -d 2000",
    "start-auth": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
